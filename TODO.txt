1. Set up the benchmark code and integrate it into the project

2. Reproduce original 'eval' results. Produce the new 'eval' (with SSD detector) results.
   Compare using all the tracking metrics (no FPS).
